pragma solidity ^0.5.0;

contract Election {

    // create a candidate structure
    struct Candidate {

    }
    // Store accounts that have voted using a mapping

    // Store Candidates and Fetch Candidate using mapping

    // Store Candidates Count

    // create a event to fire when voted

    //create a constructor to add candidates
    constructor () public {

    }

    //function to add candidates
    function addCandidates(string memory _name) public {

    }

    //function to vote
    function vote (uint _candidateId) public {
        // require that they haven't voted before

        // require a valid candidate

        // record that voter has voted

        // update candidate vote Count

        // trigger voted event
    }

}
